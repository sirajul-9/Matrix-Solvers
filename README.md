# ⚙️ Iterative Matrix Solvers — Quick Reference

## 📂 Layout
- [`C codes/`](./C%20codes) — serial C implementations of some standard iterative solvers for large, sparse linear systems  
- [`OpenMP_codes/`](./OpenMP_codes) — OpenMP parallelized versions of the same algorithms  
- [`Python_codes/`](./Python_codes) — Python (NumPy) implementations  

## 🚀 Build & Run
Place `mat.txt` (row-major) and `vec.txt` in the solver folder.

🔍 **More details:** See the individual `README.md` files inside each folder for solver descriptions and usage examples.
